Requirements
# OCR
pytesseract
pdf2image
pdfplumber

# NLP and keyword extraction
nltk
keybert
sentence-transformers    # Needed for KeyBERT to use all-MiniLM-L6-v2
sklearn - tfidfvectorizer, countvectorizer, cosinesimilarity
# Environment variables
python-dotenv

# Google Gemini API
google-generativeai

# Standard libraries (string, os) are built into Python, no need to install